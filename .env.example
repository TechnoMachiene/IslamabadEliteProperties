# ──────────────────────────────────────────────────────────────
# IEP — Environment Variables
# Copy this file to .env.local for local development.
# Set these as Environment Variables in Netlify:
#   Site settings → Environment variables
# ──────────────────────────────────────────────────────────────

# ── Supabase (SERVER-SIDE ONLY — never use VITE_ prefix here) ─
# Found in: Supabase Dashboard → Project Settings → API
SUPABASE_URL=https://hmgmstsjuqfazrhioady.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImhtZ21zdHNqdXFmYXpyaGlvYWR5Iiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc3NzcyNTAxOSwiZXhwIjoyMDkzMzAxMDE5fQ.kLItEpvFH8ailoM6LycCCrFk1S1jgVc4eWJWq7emlDs

# ── Admin panel password ───────────────────────────────────────
# Set a strong password. Changing this value and redeploying
# immediately invalidates all existing admin sessions.
ADMIN_SECRET_KEY=3i2e1p123?

# ── Frontend (optional) ────────────────────────────────────────
# Only set this if you are running Netlify Functions separately
# from the Vite dev server (e.g., netlify functions:serve on a
# different port). Leave blank when using `netlify dev`.
# VITE_API_BASE=http://localhost:9999/.netlify/functions
